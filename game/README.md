# Game Runtime

This directory is reserved for the Godot runtime layer and the integrated WebGL runtime.

The first vertical slice should consume `../data/diplomacy_rules.json` without coupling the data model to UI or online services. The existing WebGL build remains a parallel playable/runtime layer while systems are progressively unified.
