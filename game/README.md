# Game Runtime

This directory is reserved for the Godot runtime layer.

The first vertical slice should consume `../data/diplomacy_rules.json` without coupling the data model to UI or online services.
